/**
 * Typed client to the internal Core API (Identity, Data Layer,
 * Notifications). Reserved per Product Architecture §3/§19 — this is
 * the package that later becomes the base of a public SDK once a
 * Developer Portal product is justified (currently Latent). Empty
 * until apps/marketing or a future product needs its first real
 * endpoint — no speculative client code before there's a server to
 * call.
 */

export {};
