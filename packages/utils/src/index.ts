/**
 * Domain-agnostic helpers shared across apps (date formatting, string
 * utilities, validation primitives). Intentionally empty at this stage —
 * populated as real, repeated needs surface across apps/marketing and
 * future apps, not pre-built speculatively (Business First, Product
 * Architecture §9: "не будувати конфігурованість наперед").
 */

export {};
